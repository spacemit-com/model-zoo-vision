# Fire detection app entry. Model yaml uses src/ schema.

model: yolov8_fire.yaml

# Camera settings apply only with --use-camera; command-line options override them.
camera:
  backend: opencv              # This application supports OpenCV capture only.
  device: /dev/video0
  width: 1280
  height: 720
  fps: 30
