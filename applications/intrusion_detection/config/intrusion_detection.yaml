# Intrusion detection app entry (demo + model refs).
# C++ camera input override; omit to retain the existing test_video default.
# Existing --video / --use-camera / --camera-id options take precedence.
# input:
#   type: camera
#   source: /dev/video0
#   backend: opencv  # MPP is not supported by this application.
#   width: 1280
#   height: 720
#   fps: 30

tracker_model: bytetrack.yaml
test_video: ~/.cache/assets/video/001_crowd.mp4
camera_id: 0
